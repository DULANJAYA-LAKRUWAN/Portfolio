export const metadata = {
  title: "dev-lakruwan — Admin Panel",
  description: "Personal admin panel for managing projects, clients, portfolio, and works.",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
